class Product {
  constructor(id, name, description, price, image, image2) {
    (this.id = id),
      (this.name = name),
      (this.description = description),
      (this.price = price),
      (this.image = image);
    this.image2 = image2;
  }
}
export default Product;
